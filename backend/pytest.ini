[pytest]
testpaths = tests
python_files = test_*.py
python_paths = .
addopts = -v --tb=short
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning 